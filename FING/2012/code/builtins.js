//~~~~~~~~~~
// Built-ins

// Regexps
console.log("All your bases are belong to us".replace(/are /, ''));

// Math
Math.random() < 1 && Math.random() >= 0;
Math.pow(Math.sqrt(Math.E), 2);
Math.sin(Math.PI) == 0;

// Date
Date();
Date.now();
